we use the image tag with the onload attribute where we insert a javascript code.
after loading the image we update indicating that the gif has been seen (the cat exists).
when we visit the page again we dont display it
